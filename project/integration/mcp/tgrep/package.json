{
  "name": "mcp-tgrep",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "tgrep MCP wrapper for any workspace (stdio, supervised patched tgrep serve)",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0"
  },
  "scripts": {
    "test": "node --test test/backend.test.mjs test/server.test.mjs"
  }
}
