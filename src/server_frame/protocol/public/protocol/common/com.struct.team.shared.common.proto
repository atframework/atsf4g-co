syntax = "proto3";

option optimize_for = SPEED;
// option optimize_for = LITE_RUNTIME;
// option optimize_for = CODE_SIZE;
// --cpp_out=lite:,--cpp_out=
option cc_enable_arenas = true;

import "protocol/extension/v3/xresloader.proto";

option go_package = "github.com/atframework/atsf4g-co/component/public/protocol/common";

package atframework.shared;

// 注意: 放到 public/protocol/common 下是因为 atframework.shared.config.ExcelTeamType 的
// xresloader validator 需要在 config.pb 中解析本枚举(pbdesc 下的枚举不进 config.pb)。
// 本枚举曾被定义在 pbdesc/com.struct.team.shared.proto,移动后全名和值不变,不影响线上协议。
enum EnTeamType {
  EN_TEAM_TYPE_INVALID = 0;  // 无效类型
  EN_TEAM_TYPE_NORMAL = 1;   // 普通组队
}
